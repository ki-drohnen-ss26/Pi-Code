# Datasets — regenerated by build_dataset.py / negatives.py / synth.py / build_2class.py
# from the Roboflow export. Too large for git; keep them out of the repo.
pad-dataset/
pad-dataset-neg/
pad-dataset-synth/
pad-person-dataset/
pad-negatives-bench/
people-recovered/
imx-calib/
datasets/

# Training output and per-run evaluation dumps — reproducible from the train_*.py
# scripts; the summary tables in results/ are what the documentation cites.
runs/
eval/
pose_demo/
*.log

# Large regenerable model artefacts.
# yolo11n.pt is downloaded by Ultralytics on first use; the bundle and the stock
# INT8 export are both rebuilt by make_imx_bundle.py / export.py.
yolo11n.pt
yolo11n_int8.tflite
imx_bundle.zip

# Superseded: this content now lives in the project-docs site under
# docs/landing-pad/ — see README.
docs-draft/

# Python / macOS
__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
